.DS_Store
**/.DS_Store
designs/.playwright-mcp/
**/.playwright-mcp/
*.log
.idea/
.vscode/

# Foto-Originale von Lama (441 MB, ZIP >100MB wuerde GitHub-Push sprengen) — nur Web-Derivate in assets/bg/ werden versioniert
designs/10-apple-motion/assets/Fotos-Lama/

# Frame-Sequenzen der Hero-Studien 11 + 13 (42 MB / 8,8 MB) — nicht versioniert.
# Reproduzierbar aus Kortschak-3D/render/ (k_scribble_anim.py bzw. k_folie_anim.py).
# Die Sequenz von Entwurf 14 IST versioniert (wird als Startseiten-Hero verwendet).
designs/11-scroll-hero/seq/
designs/13-foil-hero/seq/

# Iris-Quell-PSDs (Stock-Originale, 97 MB) — PNG-Derivate und Quell-Videos werden versioniert
# (Regel ging in de0eab0 verloren, als die seq-Regeln sie ersetzt statt ergaenzt haben)
designs/14-iris-hero/iris-eye-isolated*/*.psd

# Arbeitsordner fuer Bild-Uploads (Originale, gehoeren nicht ins Repo)
image-upload/

# Rohmaterial von Norbert (Briefings, Original-JPGs, 96-MB-Testvideo) —
# Web-Derivate liegen versioniert in designs/10-apple-motion/assets/img/projekte/
norbert-input/

# Formular-Zugangsdaten (Passwort!) nie ins oeffentliche Repo
designs/10-apple-motion/api/anfrage-config.php

# Umzugsunterlagen mit Auth-Code — nie ins oeffentliche Repo
umzug/
